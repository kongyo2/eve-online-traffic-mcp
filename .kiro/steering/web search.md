web検索がどうしても必要な場合は、このコマンドでgoogle検索を実行して。必ず意図/前提条件、必要なデータ・情報の詳細指示、関連するすべての背景情報、定義、十分な詳細の提供を検索指示プロンプトに含めるように。## Gemini Search

`gemini` is google gemini cli. You can use it for web search.

Run web search via Task Tool with `gemini -p 'WebSearch: ...'`.

```bash

gemini -p "WebSearch: ..."

```